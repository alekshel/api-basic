{
  "name": "Api Basic",
  "description": "Api Basic",
  "image": "heroku/python",
  "repository": "https://github.com/alekshel/api-basic",
  "keywords": ["python", "django"],
  "env": {
    "DJANGO_SECRET_KEY": {
      "description": "The secret key for the Django application.",
      "generator": "secret"
    }
  },
  "environments": {
    "test": {
      "scripts": {
        "test": "./manage.py test --debug-mode"
      }
    }
  }
}
